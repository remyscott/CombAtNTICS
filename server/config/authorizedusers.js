export const authorizedUsers = {
  // This file is gitignored, so you can put real usernames and passwords here without worrying about sharing them by accident. The server will only check the username and password if ENABLE_AUTH is set to true in the environment variables (see package.json scripts).
  // Example:
  // "Alice": "password123",
  // "Bob": "hunter2"
  'User': 'abc123'
};